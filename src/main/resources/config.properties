# Browser Configuration
browser=chrome

# Application URL (not used when tests navigate via TestUrls)
baseUrl=https://demoqa.com

# Timeout Configuration (in seconds)
implicitWait=10
explicitWait=15
pageLoadTimeout=30
